/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost', '10.144.119.175', '10.144.122.201'],
        // domains: ['localhost'],

        

      },
};

export default nextConfig;
